{
  "name": "marshalederman.com",
  "scripts": {
    "start": "rimraf dist/ && mkdir dist && node index.js",
    "build": "NODE_ENV=production node index.js && cp images dist/images"
  },
  "version": "1.0.0",
  "main": "index.js",
  "author": "Zach Schwartz",
  "license": "MIT",
  "dependencies": {
    "es6-template-render": "^1.3.1",
    "gh": "^1.17.2",
    "request": "^2.88.0",
    "rimraf": "^2.6.3"
  }
}
